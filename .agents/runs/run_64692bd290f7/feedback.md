Workspace trust delayed Claude startup; verify the trust menu before task injection.
The Codex launcher selected an exhausted account and substituted Luna Reserve; verify the actual model before accepting lane output.

- Claude cold-start injection lost preambles. Initialize the terminal, wait for idle, then use worker-start --terminal.
- Verify a lifecycle heartbeat before implementation. A ready receipt alone did not prove valid worker credentials.

- Capture native seek input separately from later display values; retain failed-step diagnostics.
- Compare final lint warnings against baseline counts; a passing targeted command can still add warnings.
- Use an ISO clock for run records; macOS date does not support every fractional-time format.
