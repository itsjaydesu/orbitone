# Feedback

Preinstall with `pnpm install --frozen-lockfile --ignore-scripts`; the first test command can trigger dependency lifecycle scripts.
Write ESLint output to `.txt`; ESLint can read an incomplete `.json` report during its own run.
