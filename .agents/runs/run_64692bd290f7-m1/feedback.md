JSDOM requires a File boundary adapter that returns buffers from its own realm.
The existing Lightpanda installer downloaded a browser; further installs disabled lifecycle scripts.
Compile browser probes as standalone scripts to avoid missing tsx helper functions.
